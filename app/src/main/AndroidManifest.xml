<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expensetrackersystem">

    <application
        android:icon="@mipmap/ic_expense_tracker"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_expense_tracker"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTrackerSystem">
        <activity
            android:name=".ui.activities.profile.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.MainActivity" />
    </application>

</manifest>